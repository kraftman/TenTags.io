
<div>
  <a href="<%= url_for("sendmessage",{username = params.username}) %>">Send Message</a>
</div>

<% for i, comment in pairs(comments) do %>
  <% render("views.st.comment",{comment = comment}) %>
<% end %>
