<a class='post-icon-link' href="<%= post.link or url_for('viewpost',{postID = post.shortURL or post.id}) -%>">

  <img class = 'post-icon'
        src="<%= url_for('postIcon',{postID = post.shortURL or post.id}) -%>"
        height="50"
        width="50"
  >
</a>
